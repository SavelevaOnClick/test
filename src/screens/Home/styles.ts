import {StyleSheet} from 'react-native';
import {top} from '@constants';

export default StyleSheet.create({
  container: {
    padding: 10,
    paddingVertical: 100,
    paddingTop: top,
  },
  forGrad: {
    flex: 1,
  },
  listContainer: {
    paddingVertical: 50,
  },
  buttonWrapper: {
    alignItems: 'center',
  },
});
